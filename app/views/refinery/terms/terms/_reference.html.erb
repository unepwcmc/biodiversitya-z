<li>
    <% if reference.url.blank? %>
        <%= content_tag :span, reference.title, id: "citation-#{(reference_counter+1)}" %>
    <% else %>
        <%= link_to reference.title, reference.url, id: "citation-#{(reference_counter+1)}" %>
    <% end %>
</li>