<% content_for :body do %>
  <ul id="themes">
    <% @themes.each do |theme| %>
      <li>
        <%= link_to theme.title, refinery.terms_theme_path(theme) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
