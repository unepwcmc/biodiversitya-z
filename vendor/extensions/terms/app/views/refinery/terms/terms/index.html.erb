<% content_for :body do %>
  <ul id="terms">
    <% @terms.each do |term| %>
      <li>
        <%= link_to term.title, refinery.terms_term_path(term) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
